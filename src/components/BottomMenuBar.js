import React from 'react';
import { BottomTabBar } from '@react-navigation/bottom-tabs';

const BottomMenuBar = (props) => {
  // You can customize your Bottom Menu Bar here
  return (
    <BottomTabBar {...props} />
  );
};

export default BottomMenuBar;
